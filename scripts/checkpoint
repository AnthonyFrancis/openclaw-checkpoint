#!/bin/bash
# checkpoint - Save OpenClaw state to remote repository

set -e

WORKSPACE_DIR="${WORKSPACE_DIR:-$HOME/.openclaw/workspace}"
REMOTE_URL="${CHECKPOINT_REMOTE:-origin}"
BRANCH="${CHECKPOINT_BRANCH:-main}"

cd "$WORKSPACE_DIR" || {
    echo "❌ Workspace not found at $WORKSPACE_DIR"
    exit 1
}

# Check if git repo exists
if [ ! -d ".git" ]; then
    echo "❌ Not a git repository. Run 'checkpoint-init' first."
    exit 1
fi

# Check if there are changes to commit
if git diff --quiet HEAD && git diff --cached --quiet; then
    echo "⏭️  No changes to checkpoint"
    exit 0
fi

# Add all changes
echo "💾 Saving checkpoint..."
git add -A

# Commit with timestamp
git commit -m "checkpoint: $(date '+%Y-%m-%d %H:%M')" --quiet

# Push to remote
echo "☁️  Pushing to $REMOTE_URL/$BRANCH..."
if git push "$REMOTE_URL" "$BRANCH" --quiet; then
    echo "✅ Checkpoint saved successfully"
    echo "   Time: $(date '+%Y-%m-%d %H:%M:%S')"
    echo "   Commit: $(git rev-parse --short HEAD)"
else
    echo "❌ Failed to push checkpoint"
    echo "   You may need to pull first: checkpoint-resume"
    exit 1
fi
